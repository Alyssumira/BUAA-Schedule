<?xml version="1.0" encoding="utf-8"?>
<!--
  「今明课表」4×2 两栏组件：左栏今天、右栏明天，并排对照。
  每栏一整块文本、每行一节课（上课时刻 + 短名），刻意不用 ListView：
  一个组件里挂两个 RemoteViewsService 适配器时，宿主按 Intent.filterEquals 判等
  （它不含 extras），两份 adapter 会互相顶掉（翻周那个坑同源，见 WeekGridFactory）。
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@android:id/background"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitXY"
        android:importantForAccessibility="no"
        android:contentDescription="@null"
        android:src="@drawable/widget_bg_r20" />

    <LinearLayout
        android:id="@+id/widget_root"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:paddingStart="10dp"
        android:paddingEnd="10dp"
        android:paddingTop="8dp"
        android:paddingBottom="8dp">

        <LinearLayout
            android:id="@+id/widget_header"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <TextView
                android:id="@+id/widget_title"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                tools:ignore="NestedWeights"
                android:text="@string/widget_two_day_label"
                android:textColor="#FFFFFF"
                android:textSize="13sp"
                android:textStyle="bold"
                android:maxLines="1"
                android:ellipsize="end" />

            <TextView
                android:id="@+id/widget_subtitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="6dp"
                android:text=""
                android:textColor="#C9D6EE"
                android:textSize="11sp"
                android:maxLines="1" />
        </LinearLayout>

        <!--
          两栏各占一半：栏头是「今天 周四」，正文一整块按行下发。
          容器 0dp + weight：竖向 LinearLayout 里 match_parent 会把表头那几十 dp 也
          算进自身高度，两栏底部就画到组件外被裁掉。
          正文的 maxLines 只是**上限**（TWO_DAY_MAX_LINES_CEILING），实际画几行由
          WidgetCommon 按组件当前高度算出来再截断（两栏用的是整块文本，不是 ListView，
          RemoteViews 也没有 setMaxLines 这条 action，只能把布局放到顶、由数据侧收）。
        -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:orientation="horizontal"
            android:baselineAligned="false"
            android:layout_marginTop="4dp">

            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:orientation="vertical"
                tools:ignore="NestedWeights">

                <TextView
                    android:id="@+id/widget_column_title_left"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="@string/widget_column_today"
                    android:textColor="#FFFFFF"
                    android:textSize="11sp"
                    android:textStyle="bold"
                    android:maxLines="1"
                    android:ellipsize="end" />

                <TextView
                    android:id="@+id/widget_column_body_left"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="2dp"
                    android:lineSpacingExtra="2dp"
                    android:text="@string/widget_none_placeholder"
                    android:textColor="#E8F0FE"
                    android:textSize="11sp"
                    android:maxLines="8"
                    android:ellipsize="end" />
            </LinearLayout>

            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:layout_marginStart="8dp"
                android:orientation="vertical"
                tools:ignore="NestedWeights">

                <TextView
                    android:id="@+id/widget_column_title_right"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="@string/widget_column_tomorrow"
                    android:textColor="#FFFFFF"
                    android:textSize="11sp"
                    android:textStyle="bold"
                    android:maxLines="1"
                    android:ellipsize="end" />

                <TextView
                    android:id="@+id/widget_column_body_right"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="2dp"
                    android:lineSpacingExtra="2dp"
                    android:text="@string/widget_none_placeholder"
                    android:textColor="#E8F0FE"
                    android:textSize="11sp"
                    android:maxLines="8"
                    android:ellipsize="end" />
            </LinearLayout>
        </LinearLayout>

    </LinearLayout>

</FrameLayout>
