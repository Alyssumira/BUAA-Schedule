<?xml version="1.0" encoding="utf-8"?>
<!--
  今日/明日课程组件（列表型）。
  - 背景层是独立 ImageView：RemoteViews 只能 setColorFilter/setAlpha/setImageResource
  - 根背景用 @android:id/background：官方要求，用于「点组件启动应用」的过渡动画
  - ListView + setEmptyView：真滚动列表，空态由 widget_empty 呈现
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@android:id/background"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitXY"
        android:importantForAccessibility="no"
        android:contentDescription="@null"
        android:src="@drawable/widget_bg_r20" />

    <LinearLayout
        android:id="@+id/widget_root"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:paddingTop="10dp"
        android:paddingBottom="10dp">

        <LinearLayout
            android:id="@+id/widget_header"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <TextView
                android:id="@+id/widget_title"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="@string/widget_today_label"
                android:textColor="#FFFFFF"
                android:textSize="13sp"
                android:textStyle="bold"
                android:letterSpacing="0.02"
                android:maxLines="1"
                android:ellipsize="end" />

            <TextView
                android:id="@+id/widget_subtitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text=""
                android:textColor="#C9D6EE"
                android:textSize="11sp"
                android:maxLines="1" />
        </LinearLayout>

        <!--
          0dp + weight：竖向 LinearLayout 里 match_parent 是"照父高量一次"，
          表头占掉的那几十 dp 不会从列表高度里扣出去 —— 于是最后几行画在组件外被裁掉。
          空态与列表由 setEmptyView 互斥，同一时刻只有一个可见，权重不冲突。
        -->
        <ListView
            android:id="@+id/widget_list"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:layout_marginTop="4dp"
            android:divider="@null"
            android:dividerHeight="0dp"
            android:scrollbars="none" />

        <TextView
            android:id="@+id/widget_empty"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:gravity="center"
            android:text="@string/widget_empty_today"
            android:textColor="#E8F0FE"
            android:textSize="12sp" />

    </LinearLayout>

</FrameLayout>
